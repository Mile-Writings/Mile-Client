import styled from '@emotion/styled/macro';

const Curious = () => {
  return (
    <CuriousGroupText>
      이 모임에 대해서
      <br /> 더 궁금하신가요?
    </CuriousGroupText>
  );
};

export default Curious;

const CuriousGroupText = styled.div``;
